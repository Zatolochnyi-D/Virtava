[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "server"
version = "0.0.1"
dependencies = [
  "pyzmq==27.1.0",
  "protobuf",
]
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]