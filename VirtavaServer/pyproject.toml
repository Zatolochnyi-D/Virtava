[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "virtava_server"
version = "0.0.1"
dependencies = [
  "pyzmq==27.1.0",
  "protobuf<5,>=4.25.3",
  "grpcio-tools"
]
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]